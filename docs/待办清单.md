# ImagePilot 待办清单

> 更新时间：2026-06-22 · 当前版本 **v1.5.66**（iOS/Android 功能对齐，双端真机验证；近期连发：地区选源/反馈入口/crash修复/clip引导/端侧润色/防自动锁屏）

## 🅿️ 等用户 / 外部（暂时推不动）

- [ ] **iOS App Store 上架** — 材料全就绪（文案/隐私/截图/build 号，见 `docs/platforms/iOS上架文案与隐私标签.md`）；**卡点根因已查明（不是身份证拍照问题）**：用户的身份验证**此前已绑定到一个 Apple Developer Enterprise（$299，仅内部分发、不能上架 App Store）账号**，用户是该账号持有人。Apple 规则「一个人的身份只能当一个账号的持有人」→ 用户名下**任何** Apple ID（国内 sina/qq、海外 gmail）注册个人账号都被拒、Program 入口变灰（**换自己的号无用，身份绑人不绑号/不绑地区**）。**解决路径**：① 回信问 Apple（case 102918906042）能否解绑/重置身份验证，或转让 Enterprise 持有人/注销账号释放身份（最省事）；② Plan B：用**至亲的身份+Apple ID**注册个人账号（app 归属那人、需用其 Apple ID 登录操作、个人账号不能加成员、付款收款归那人）。**用户已决定「重新申请一个开发者账号后再继续」，iOS 上架暂挂起。** 账号通过后：付费 → build 号 +1（3→4）→ Archive → 上传 → Connect 填文案 → 销售区香港+海外（排除大陆，大陆需 ICP 备案）→ 提审。
- [ ] **付费功能基建**（用户规划「先免费攒用户 → 后期收费，老用户永久免费」）— **完整策略 + 执行清单见 `docs/platforms/收费策略与运营.md`**。走 Freemium；老用户用 StoreKit `originalApplicationVersion` 做版本分界豁免；**前期零技术准备**，现维持「免费、无内购」（YAGNI）。真要做再接 RevenueCat/StoreKit + 签付费协议 + 银行税务 + 改文案/隐私问卷。
- [ ] **华为"恶意安装"申诉** — 用户侧在华为开发者后台提交（文档 `docs/platforms/华为安装拦截-误报申诉.md` 已备）。

## ⏳ 待用户真机验证（已发版，等反馈调整）

- [ ] **端侧大模型润色效果**（v1.5.65）— 时刻秀润色/搜索改写优先端侧 Gemma（回退云端）。前提先下 VLM 模型（2.6GB）。**待用户测端侧润色质量/速度**——不理想则调 prompt 或加质量兜底（架构保证端侧出问题不影响云端）。
- [ ] **防自动锁屏**（v1.5.66）— 下载/分类时屏幕常亮、任务不被自动锁屏中断。**待用户测**（自动锁屏调 30 秒看下载/分类是否不中断）。

## 🔧 可选 · 用户点了才做（已后置）

- [ ] **PC 端功能拉齐**（后续开搞）— 完整可行性评估 + 分档 + 路径见 **`docs/platforms/PC端功能拉齐规划.md`**。结论：可行性大（逻辑已共享、PC 端 onnxruntime-node 已装）。优先做 🟢 档（回忆/应用内放映/语义搜索/相册报告/去重/备份，纯 desktop UI）；PC 大模型能力**走云端**（零新基建）；🔴 档（时刻秀导出 MP4 需 ffmpeg、本地大模型推理栈需 llama.cpp/ollama、美颜需 ONNX 人脸）单列按需做。
- [ ] **时刻秀模板二期 2b** — 运镜（Ken Burns/平移）、文字动画（打字机/弹入）、节拍卡点（音频分析）。要双端原生逐帧重写（iOS AVAssetWriter + 安卓 MediaCodec），**风险高、耗时多**，建议真机迭代。（2a 多画幅+排版已发）
- [ ] **VLM 多模态分类档**（llama.rn + SmolVLM-500M / Qwen3-VL-2B） — 有完整实现计划（`docs/superpowers/plans` / 计划文件）；要先做 RN 0.72 兼容性 spike，多天工程。
- [ ] **图标走查第三批** — 剩数据驱动的修图预设 emoji + 空状态大图标，价值低。
- [ ] **CLIP 中文文本搜图** — 现 mobileclip 文本塔纯英文；要做须换 Chinese-CLIP 全套 + 重建索引，已搁置。

## ✅ 本轮已完成（v1.5.46 → v1.5.61，摘要）

- 时刻秀：模板一键出片（25 套 + LUT 调色 + 标题卡）、导出视频（双端含配乐）、编辑（增减图/封面/换封面）、多画幅（9:16/16:9/1:1）、标题卡排版预设
- 修图：美颜换 ML Kit（安卓不漏脸）、修图深度拉杆、清晰增强返回不卡、读秒移除
- 修复：向量索引 >100%/补全无反应、无 id 照片可删、✨ 生成描述 JSON 报错、相册报告分类名、长按看不到删除、编辑覆盖 bug、iOS 模板套用偶发闪退
- 图标走查第一、二批；文档全面更新；iOS/安卓功能对齐
- **#26 分类模型切 ModelScope 源**（v1.5.60）：10 个端侧模型上传到魔搭 `chunguangwee/ImagePilot-models`，classify/enhance 下载 URL 切 ModelScope。
- **模型按地区自动选源**（v1.5.61，为全球上线）：国内 ModelScope 优先 / 海外 GitHub 优先，互为兜底。
- **app 内意见反馈入口**（v1.5.62）：设置→关于，一键发邮件预填版本/系统诊断信息，双端。
- **iOS 模型下载闪退修复**（v1.5.63）：modelRegion 误引未安装的 react-native-localize → 未捕获 JS 致命异常；改用 RN 内置系统 locale + 下载选源防御兜底。全项目扫描确认无其它同类缺失依赖。
- **整库扫描后引导升级 clip**（v1.5.64）：默认 basic 对抽象场景有限，扫完弹一次性引导卡推荐端侧 clip（一键跳设置下载，永不重复打扰）。
- **端侧大模型润色**（v1.5.65）：时刻秀润色/搜索改写优先端侧 Gemma、回退云端（复用 VLM + 占位图技巧）。
- **运行时防自动锁屏**（v1.5.66）：下载/分类时保持屏幕常亮（iOS isIdleTimerDisabled / 安卓 FLAG_KEEP_SCREEN_ON），任务不被自动锁屏中断；结束恢复省电。

## 当前状态
仓库干净、main 与远端同步、**v1.5.66 全平台已发版**、无已知 bug。功能侧近期连发 6 版均双端验证。**主线唯一硬卡点：iOS 上架的 Apple Developer 账号身份验证**（用户与 Apple 处理中）。
