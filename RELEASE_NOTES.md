ImagePilot 1.5.79 更新要点

本次为功能优化版：

• 相似照片检测支持「增量检测 / 全部检测」二选一：
  在「相似照片」点「重新检测」时，会弹窗让你选择——
  - 增量检测（仅新增）：只比对新增的照片，已比对过的照片结果保留，照片很多时速度明显更快；
  - 全部检测：重新比对全部照片（原来的行为）。
  首次检测（还没有任何历史记录）会自动按全部检测处理。

• 「最新发现照片」头部去掉了多余的数字，界面更清爽。

感谢使用 ImagePilot，欢迎在「设置 → 检查更新」获取最新版本。

---

ImagePilot 1.5.79 — What's New

Feature improvements:

• Similar-photo detection now lets you choose Incremental or Full:
  When you tap "Re-check" under Similar Photos, you can pick—
  - Incremental (new only): compares only newly added photos while keeping existing results, which is much faster on large libraries;
  - Full scan: re-compares all photos (the previous behavior).
  The first run (no history yet) automatically falls back to a full scan.

• Removed redundant numbers in the "Recently Discovered" header for a cleaner layout.

Thanks for using ImagePilot. Check Settings → Check for Updates to get the latest version.
